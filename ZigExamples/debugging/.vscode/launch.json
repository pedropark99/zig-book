{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(lldb) Launch",
            "type": "cppdbg",
            "request": "launch",
            "cwd": "${workspaceFolder}",
            "program": "${workspaceFolder}/zig-out/bin/debugging",
            "stopAtEntry": true,
            "externalConsole": false,
            "miDebuggerPath": "/usr/bin/lldb",
            "preLaunchTask": "build"
        }
    ]
}